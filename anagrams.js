'use strict';

function anagrams(word){
  // Base Case
  if (iterate === word.length){
    return;
  }
	// Recursive Case
	for (let i=0; i<word.length; i++){
		word[0]
	}
  anagrams(word.slice(1) + word[0]);
}

anagrams('play', );